# bi-scripts

# Установка

``` bash
sudo apt-get update
sudo apt-get upgrade
sudo apt-get install python3 python3-pip python3-virtualenv git libpq-dev
git clone 
cd bi-scripts
python3 -m virtualenv env
source env/bin/activate
pip3 install -r requirements.txt
```